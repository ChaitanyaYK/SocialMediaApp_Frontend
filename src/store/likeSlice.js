import { createSlice } from "@reduxjs/toolkit";
import axios from "axios";

const initialState = {
    
}

const likeSlice = createSlice({
    name: 'likes',
    initialState,
    reducers: {

    }
})